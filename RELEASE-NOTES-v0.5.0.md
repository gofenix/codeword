# CodeWord v0.5.0 — 大重构

> 6 个真问题都修了。

## 6 个问题

### 1. 数据丢失 ❌ → ✅
**原因**：数据本来就在 `getApplicationDocumentsDirectory()` 持久化（macOS `~/Library/Containers/<bundle>/Data/Documents/`、Android `/data/data/<package>/app_flutter/`），**但你之前可能在覆盖装之前先卸载了**。卸载会清掉 app 数据目录。

**修法**：在「我的 → 本地数据」里直接显示当前数据路径，**让你自己能看到存到哪**。如果你覆盖装（不卸载），数据就一直在。

### 2. 版本号 1.0.0 ❌ → ✅
**原因**：pubspec.yaml 里 `version: 1.0.0+1` 是 `flutter create` 留的默认值，我从来没动过。

**修法**：`version: 0.5.0+12`（build number = release 序号）。现在关于页能正确显示 0.5.0。

### 3. 复习 tab ❌ → ✅
**修法**：tab 删了。复习这个概念直接**融进学习流** —— 见 #6。

### 4. 记忆曲线 ❌ → ✅
**之前**：进学习流是 `从词库随机抽 10 个词`。

**现在**：70% 新词 + 30% 复习到期词（SM-2 due ≤ now），按到期优先级排队。词库新词抽完再 backfill 已学过的词。

例：你今天进 AI 核心，可能拿到 7 个新词 + 3 个你之前答错过现在到期的词。答完后这 10 个的 SM-2 状态都更新。

### 5. 词库集成到「我的」 ❌ → ✅
**修法**：词库 tab 删了。9 套词库现在作为**「我的」里的一个 section**（"词库"小标题 + 2 列卡片网格），点卡片直接进学习流。设置（每日新词数 / 发音 / 关于 / 本地数据）也都在「我的」里。

### 6. 5 个 tab 重排 ❌ → ✅
**之前**：今日 / 词库 / 复习 / 统计 / 我的
**现在**：**单词 / 阅读 / Pulse / 统计 / 我的**
- **单词** = 之前"今日"，是学习流入口
- **阅读** = 占位 tab（功能没做，安安静静一个图标）
- **Pulse** = 占位 tab（同上）
- **统计** = 不变
- **我的** = 词库 + 设置 + 关于 + 本地数据 都在这里

⚠️ **关于「阅读」和「Pulse」**：你只说了名字没说具体是什么。我做了**安静占位**（就一个图标，不放"敬请期待"那类话术）。如果你想要别的，告诉我具体是什么。

## 改动清单

| 项 | 之前 | 现在 |
|---|---|---|
| pubspec version | 1.0.0+1 | 0.5.0+12 |
| tab 1 | 今日 | **单词** |
| tab 2 | 词库 | **阅读**（占位） |
| tab 3 | 复习 | **Pulse**（占位） |
| tab 4 | 统计 | 统计 |
| tab 5 | 我的 | 我的（**含词库**） |
| 学习流来源 | 随机 10 词 | **70% 新 + 30% 到期复习** |
| 数据位置 | 不可见 | 「我的 → 本地数据」显示路径 |
| 词库 tab | 独立 tab | 整合到「我的」section |
| 复习 tab | 独立 tab | **删除**（融入学习流） |
| library_screen.dart | 8 测过的导入 | 删了 |
| review_screen.dart | 8 测过的导入 | 删了 |

## 8 测过

无变化（数据流/UI 改，data layer 没改）。

## 下载

| 平台 | 链接 | 大小 |
|---|---|---|
| macOS | [codeword-0.5.0-macos.dmg](https://github.com/gofenix/codeword/releases/download/v0.5.0/codeword-0.5.0-macos.dmg) | 22 MB |
| Android | [app-0.5.0-release.apk](https://github.com/gofenix/codeword/releases/download/v0.5.0/app-0.5.0-release.apk) | 46 MB |

## 装上 v0.5.0 验

1. **版本号**：「我的 → 关于」应该显示 `CodeWord` 不再带 1.0.0 字样
2. **本地数据路径**：「我的 → 本地数据」应该显示一个 macOS 或 Android 路径
3. **tab 1** 是「单词」（不是「今日」）
4. **tab 2/3** 是「阅读」「Pulse」，安静占位
5. **tab 5「我的」** 往下滚看到**词库**（9 套卡片）
6. **点词库卡片** → 直接进学习流
7. **学习流内容**：先答几个词，再进学习流 → 应该**混进 1-2 个你之前答错的词**（如果到期了）
8. **数据持久性**：装上 v0.5.0，**别卸载旧的**，直接覆盖装 → 数据应该在

## 还需要你回答

「**阅读**」和「**Pulse**」具体是干啥的？告诉我后我直接做。

挑下一个。
