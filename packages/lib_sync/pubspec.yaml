name: lib_sync
description: 6-digit sync code + E2E encryption for CodeWord.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  pointycastle: ^3.9.1
  lib_core:
    path: ../lib_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
