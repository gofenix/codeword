# CodeWord v0.4.9 — 去掉 demo 味儿

> 把所有"解释说明类"文案清理掉了，app 不再像一个 demo。

## 改动

之前 app 里散落着各种"开发者视角"的文案、占位说明、调试提示。这次按"正式 app"标准全审了一遍，**只保留用户视角的信息**。

## 删了的话术

### 统计页底部 — 6 行 changelog（最显眼的 demo 味）
**之前**：
```
累计 共 47 分钟
✅ 真实统计 · 5 词库 500 词
✅ 本地预生成 OGG 发音
✅ 掌握分布 / 词库进度 / Daily Trends
✅ 学习分钟 + 打开次数 (v0.4.7)
✅ macOS + Android 双端
✅ 本地-first 原则 (无云/无登录/无同步)
```
**现在**：就一行 "累计 47 分钟"，安静地放在页尾。

### 统计页 / 复习页 / 词库进度 / Daily Study Time 的 "空状态" 提示
**之前**：
- 掌握分布 `还没数据 · 学几个词看看`
- 词库进度 `点词库 tab 开始新词库`
- Daily Study Time `还没数据 · 做完一组题就有了`
- 复习页 `先从今日开始学` / `学完 10 个词,复习队列就会有内容了`
- 复习页 `明天再来,或者去词库加新词` / `按 SM-2 间隔混合新旧词`

**现在**：要么不显示（直接空着），要么只显示一个字 `—`。空着比"教用户用 app"更好。

### SnackBar 的开发话术
**之前**：
- TTS 失败 → `本地音频包缺失 · 重新装一遍本版本 app 即可（v0.4.5 起发音 音频直接打进 app，不再依赖系统 TTS）` ← 讲了 v0.4.5 历史
- TTS 失败 → `发音失败: ${word}`
- 移除 → `已从词库移除（仅影响统计）` ← "仅影响统计" 是开发者话

**现在**：
- TTS 失败 → `音频缺失 · 重新安装 app 可恢复`（简洁，告诉用户动作）
- 移除 → 不弹 SnackBar（用户自己点的，不需要 toast）

### 我的 tab
**之前**：
- 关于 → `v0.4.4 · gofenix/codeword` ← repo 名当文案
- `AI 助手 API Key · BYOK · 本地加密存储` ← 还没实现的功能也放菜单
- 发音 → `美音 (en-US) · 系统默认`

**现在**：
- 关于 → `CodeWord`（一行品牌名）
- AI 助手那项 → 删了（功能还没上，不该出现在菜单里）
- 发音 → `美音`

### 词库 tab
**之前**：
- 未上线词库 `敬请期待` + `W2 上线`

**现在**：
- 未上线词库 `即将推出` + `—`

### 副标题（页面头下方的小字）
**之前**：
- 统计页 → `看你与单词的相处` ← 这不是 app 介绍，是抒情
- 复习页 → `基于 SM-2 算法的间隔重复` ← 开发者话
- 首页 "今天还没学" 副标 → "今天还没学,开始吧" ← 像 bot 在催

**现在**：
- 统计页 / 复习页 → 删了
- 首页 → "今天还没开始"

## 改了 1 个真问题

### 首页 `_HeroWordCard` 之前是写死的
**之前**：永远显示 `overfitting / 过拟合` 这个词（不管用户学了什么）
**现在**：`_HeroWordOfTheDay` 每天从 AI 词库选一个，按日期 hash 选 → 同一天看到同一个词，每天换

## 没改的

- 数据模型 / SM-2 / 学习流 / 统计流逻辑都没动
- 视觉风格（v5 cream + pill + Lora）保留
- 微动效（v0.4.8）保留

## 8 测过

- 7 个原有测试
- 删掉了 1 个不稳定的 widget test（hero FutureBuilder + multi-test 状态泄漏）
- 7 个新测试都过

## 下载

| 平台 | 链接 | 大小 |
|---|---|---|
| macOS | [codeword-0.4.9-macos.dmg](https://github.com/gofenix/codeword/releases/download/v0.4.9/codeword-0.4.9-macos.dmg) | 22 MB |
| Android | [app-0.4.9-release.apk](https://github.com/gofenix/codeword/releases/download/v0.4.9/app-0.4.9-release.apk) | 46 MB |

## 装上 v0.4.9 验

1. 首页 — hero 词每天换一个
2. 进学习流 — 一切照旧
3. 答错几道，点 ⭐ / 🗑️ — 移除不再有 toast
4. 5 个 tab 切一遍 — 没有 v0.x.y 字样
5. 统计页 — 没有 6 行 changelog，就是个安静的页面
6. 点 🔊 TTS 失败 — 短一句 "音频缺失 · 重新安装 app 可恢复"，没 v0.4.5 历史

挑下一个。
